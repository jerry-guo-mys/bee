# 工作台需求 — 开发进度

> 用于**暂停 / 续作**：记录已完成步骤、当前分支、下一步与验证方式。  
> 与 [开发计划](./工作台需求-开发计划.md) 同步更新勾选状态。

---

## 当前状态（续作时先看这里）

| 项 | 值 |
|----|-----|
| **最后更新** | 2026-04-02 |
| **已完成里程碑** | **M1**（前端信息架构：工作台 vs 平台管理） |
| **下一步** | **M2** — 工作流模板持久化（Postgres/SQLite DDL、合并内置模板、专家 API、AgentInstanceResolver、`start` 改造） |
| **建议分支** | `feature/workbench`（本地若未建，可从当前 main 拉出） |

---

## M1 完成记录

### 已实现

- 路由 **`/workbench/runs`**：流程与任务页（复用原 `Workflows` 组件）。
- **`/workbench`**：重定向到 `/workbench/runs`。
- **`/workflows`**：重定向到 `/workbench/runs`（兼容旧书签/外链）。
- 侧边栏分组：**工作台**（总览、流程与任务）| **平台管理**（原管理项全部归入此组）。
- 侧栏标题改为 **「Bee 控制台」**；搜索框占位文案微调。
- **流程与任务**页：标题/副文案产品化；**未填 `team_id`** 时顶部 **琥珀色提示条**，链到系统设置；启动成功文案改为「已启动流程…」。
- 顺带修复：`Teams.tsx` 创建团队时 `parent_team_id` 使用 `undefined` 而非 `null`，满足 `tsc`（与 M1 同次构建发现）。

### 涉及文件

- `web-ui/src/App.tsx`
- `web-ui/src/components/Layout.tsx`
- `web-ui/src/pages/Workflows.tsx`
- `web-ui/src/pages/Teams.tsx`（类型修复）

### 本地验证（续作后可快速回归）

```bash
cd web-ui && npm run build
# 可选：npm run dev，浏览器检查侧栏分组、/workbench/runs、/workflows 重定向、无 team_id 时提示条
```

---

## 待办（按开发计划顺序）

1. **M2** — 见 [开发计划 §5](./工作台需求-开发计划.md#5-m2--工作流模板出库表--resolver--start-兼容)。
2. **M3** — TaskRepository + SQL 单数据源。
3. **M4** — 项目 + `spawn-children`。
4. **M5**（可选）— `task_kind`、Review 报告、CI 展示。

---

## 续作检查清单

- [ ] 拉最新主分支 / 合并同事变更。
- [ ] 读本文件「当前状态」与开发计划对应里程碑任务列表。
- [ ] M2 开工前建议确认：部署用 SQLite 还是 Postgres（影响迁移文件优先级）。
- [ ] 每完成一小步：在本文件「完成记录」追加小节，并更新开发计划勾选。

---

## 变更日志（简）

| 日期 | 说明 |
|------|------|
| 2026-04-02 | 完成 M1；新增本进度文档 |
